To execute the exemple you need to start the docker compose using the following command:
```bash
docker-compose up -d --build
```

Then you can access the application using the following URL:
```shell
localhost:5000
```